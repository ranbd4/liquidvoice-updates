# LiquidVoice Updates

This repository hosts release assets and the Sparkle auto-update feed for LiquidVoice.

The `appcast.xml` file is automatically updated by `scripts/release.sh` each time a new version is published.
